openapi: 3.0
components:
  schemas:
    MyType:
      description: A buggy definition
      type: object
      properties:
        myField:
          type: string
          enum: ["foo", "bar", null]
          nullable: true