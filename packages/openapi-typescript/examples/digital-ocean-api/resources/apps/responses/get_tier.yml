description: A JSON with the key `tier`

content:
  application/json:
    schema:
      $ref: ../models/apps_get_tier_response.yml
    examples:
      tier:
        $ref: examples.yml#/tier

headers:
  ratelimit-limit:
    $ref: ../../../shared/headers.yml#/ratelimit-limit
  ratelimit-remaining:
    $ref: ../../../shared/headers.yml#/ratelimit-remaining
  ratelimit-reset:
    $ref: ../../../shared/headers.yml#/ratelimit-reset
