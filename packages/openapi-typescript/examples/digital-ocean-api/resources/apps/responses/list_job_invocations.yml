description: A JSON with key `job_invocations`

content:
  application/json:
    schema:
      $ref: ../models/app_job_invocations.yml
    examples:
      job_invocations:
        $ref: examples.yml#/job_invocations

headers:
  ratelimit-limit:
    $ref: ../../../shared/headers.yml#/ratelimit-limit
  ratelimit-remaining:
    $ref: ../../../shared/headers.yml#/ratelimit-remaining
  ratelimit-reset:
    $ref: ../../../shared/headers.yml#/ratelimit-reset
