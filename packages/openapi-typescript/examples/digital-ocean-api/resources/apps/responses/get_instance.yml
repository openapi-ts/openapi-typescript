description: A JSON with key `instance_size` 

content:
  application/json:
    schema:
      $ref: ../models/apps_get_instance_size_response.yml
    examples:
      instance_size:
        $ref: examples.yml#/instance_size

headers:
  ratelimit-limit:
    $ref: ../../../shared/headers.yml#/ratelimit-limit
  ratelimit-remaining:
    $ref: ../../../shared/headers.yml#/ratelimit-remaining
  ratelimit-reset:
    $ref: ../../../shared/headers.yml#/ratelimit-reset
