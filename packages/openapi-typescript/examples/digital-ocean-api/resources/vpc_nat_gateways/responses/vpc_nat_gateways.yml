description: A JSON object with a key of `vpc_nat_gateways`.

headers:
  ratelimit-limit:
    $ref: '../../../shared/headers.yml#/ratelimit-limit'
  ratelimit-remaining:
    $ref: '../../../shared/headers.yml#/ratelimit-remaining'
  ratelimit-reset:
    $ref: '../../../shared/headers.yml#/ratelimit-reset'


content:
  application/json:
    schema:
      allOf:
        - type: object
          properties:
            vpc_nat_gateways:
              type: array
              items:
                $ref: '../models/vpc_nat_gateway_get.yml'
        - $ref: '../../../shared/pages.yml#/pagination'
        - $ref: '../../../shared/meta.yml'
    examples:
      VPC NAT Gateways List Response:
        $ref: 'examples.yml#/vpc_nat_gateways'
