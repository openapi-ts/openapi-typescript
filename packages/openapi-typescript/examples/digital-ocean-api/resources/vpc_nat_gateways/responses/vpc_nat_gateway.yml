description: |
  The response will be a JSON object with a key called `vpc_nat_gateway`. This will be
  set to a JSON object that contains the standard VPC NAT gateway attributes.

headers:
  ratelimit-limit:
    $ref: '../../../shared/headers.yml#/ratelimit-limit'
  ratelimit-remaining:
    $ref: '../../../shared/headers.yml#/ratelimit-remaining'
  ratelimit-reset:
    $ref: '../../../shared/headers.yml#/ratelimit-reset'

content:
  application/json:
    schema:
      properties:
        vpc_nat_gateway:
          $ref: '../models/vpc_nat_gateway_get.yml'
    examples:
      VPC NAT Gateway Response:
        $ref: 'examples.yml#/vpc_nat_gateway'
