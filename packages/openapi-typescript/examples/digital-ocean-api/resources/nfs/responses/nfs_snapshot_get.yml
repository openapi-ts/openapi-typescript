description: >-
  The response will be a JSON object with a key called `snapshot`. The value will
  be an object containing the standard attributes associated with an NFS snapshot.

headers:
  ratelimit-limit:
    $ref: '../../../shared/headers.yml#/ratelimit-limit'
  ratelimit-remaining:
    $ref: '../../../shared/headers.yml#/ratelimit-remaining'
  ratelimit-reset:
    $ref: '../../../shared/headers.yml#/ratelimit-reset'

content:
  application/json:
    schema:
      $ref: '../models/nfs_snapshot_get_response.yml'
