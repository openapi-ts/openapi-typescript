operationId: security_delete_suppression

summary: Delete Suppression

description: To remove a suppression, send a DELETE request to `/v2/security/settings/suppressions/{suppression_uuid}`.

tags:
  - Security

parameters:
  - $ref: 'parameters.yml#/suppression_uuid'

responses:
  '204':
    $ref: '../../shared/responses/no_content.yml'

  '401':
    $ref: '../../shared/responses/unauthorized.yml'

  '404':
    $ref: '../../shared/responses/not_found.yml'

  '429':
    $ref: '../../shared/responses/too_many_requests.yml'

  '500':
    $ref: '../../shared/responses/server_error.yml'

  default:
    $ref: '../../shared/responses/unexpected_error.yml'

x-codeSamples:
  - $ref: 'examples/curl/security_suppression_delete.yml'

security:
  - bearer_auth:
      - 'security:delete'
