GetItemOperation:
  get:
    responses:
      "200":
        description: OK
        content:
          application/json:
            schema:
              $ref: "#/Item"
Item:
  type: object
  required:
    - id
    - name
  properties:
    id:
      type: string
    name:
      type: string
