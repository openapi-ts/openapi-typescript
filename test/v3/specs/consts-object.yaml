openapi: 3.0
paths:
  /test:
    get:
      tags:
        - test
      summary: "Just a test path"
      responses:
        200:
          description: A list of types.
components:
  schemas:
    TypeA:
      type: object
      const: { hello: world }
    TypeB:
      type: array
      const: ["content"]
