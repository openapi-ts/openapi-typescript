openapi: 3.0
paths:
  /test:
    get:
      tags:
        - test
      summary: "Just a test path"
      responses:
        200:
          description: A list of types.
components:
  schemas:
    Example:
      description: Enum with null and nullable
      type: object
      properties:
        emptyAllOf:
          allOf: []
        emptyOneOf:
          oneOf: []
        emptyAnyOf:
          anyOf: []