openapi: "3.0"
info:
  title: test
  version: "1.0"
paths:
  /:
    get:
      responses:
        200:
          description: OK
components:
  schemas:
    SchemaType:
      type: object
      properties:
        foo:
          type: string
        bar:
          type: number
